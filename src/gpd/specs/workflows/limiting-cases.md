<purpose>
Execute `limiting-cases` with the shared technical-analysis workflow.
</purpose>

<operation>
Bind `ANALYSIS_OPERATION=limiting-cases`.
Phase target: `OUTPUT_PATH="${phase_dir}/LIMITING-CASES.md"`.
File target: `OUTPUT_PATH="GPD/analysis/limits-{slug}.md"`.
No shared-state update.
Read `{GPD_INSTALL_DIR}/references/analysis/limiting-cases-method.md` for this method.
</operation>

@{GPD_INSTALL_DIR}/workflows/technical-analysis.md
